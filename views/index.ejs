<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>INDEX WORKING</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
    <h1 class="web-title">KITCHEN CLASSICS</h1>
    <nav class="nav-links">
        <a class= "link" >Home</a>
        <a href="/recipes/new" class= "link" id="new">New</a>
        <a class= "link">Reccomendations</a>

    </nav>
    <ul class = 'recipe'>
        <% for (let i=0; i < allRecipes.length; i++){ %>
    <li class="recipe-item">
        <div class="recipe-title">
            <a href="/recipes/<%= allRecipes[i]._id %>" class = 'recipe-title'><%=allRecipes[i].title%></a>
            </div>
        <div class="recipe-actions">
            <a href="/recipes/<%=  allRecipes[i]._id %>/edit" class="recipe-edit">EDIT</a>
            <form id="delete-form-<%= allRecipes[i]._id %>" action="/recipes/<%= allRecipes[i]._id %>?_method=DELETE" method="POST" style="display: inline;">
                <a  href="#" onclick="document.getElementById('delete-form-<%= allRecipes[i]._id %>').submit(); return false;" class="recipe-delete">DELETE</a>
            </form>
        </div>
    </li>

            <li class="recipe-info">
            </li>
         <% } %>

    </ul>
</body>
</html>